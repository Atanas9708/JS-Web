const home = require('./home-controller');
const user = require('./user-controller');
const thread = require('./threads-controller');
const message = require('./messages-controller');

module.exports = {
  home,
  user,
  thread,
  message
};
